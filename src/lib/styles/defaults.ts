import { getTemplateStyles, DEFAULT_TEMPLATE } from './templates';

/**
 * Default style settings based on GB/T 9704-2012
 * Chinese Government Document Format Specifications
 *
 * Re-exported from templates.ts for backward compatibility
 */
export const DEFAULT_STYLES = getTemplateStyles(DEFAULT_TEMPLATE);
